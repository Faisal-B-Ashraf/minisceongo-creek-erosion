# Imagery (not stored in the repository)

Run `python analysis/2_Scripts/01_fetch_imagery.py` to download the clips of the study area from the NYS orthoimagery
service (orthos.its.ny.gov). It writes one GeoTIFF per year here (NAD83 NY State Plane East, US ft, 0.5-ft pixels).
